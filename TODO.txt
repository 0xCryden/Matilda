	General:
- Currently sending packets does not seem to be possible while not capturing. I want to be able to send the packets while not logging further packets to the table. Manually sent packets should be logged to the table independent of the capture mode. The PROC field in the table should also display whether or not the packet was sent manually or from the process itself by adding the string (Matilda)

	DATA:
- Sent packets' TCP Seq and ACK numbers seem to just be copied, they need to be corrected to be the actual numbers they should be (maybe other fields need adjustment as well? window size?) so the server will actually process the packets
- Currently when sendPackets fails, it detects a new application "Added application: Error: sendPackets failed" is in the console 

	HexView:
- The ascii displayed lines/parts are just appended to the HEX for each row but they need to have something which makes sure their beginnings are aligned because for the last line for example the ASCII is always indented to the left because the hex part is shorter. either use three different text columns while making sure their lines correspond to each other (offset, hex, ascii) OR leave them as single rows for all three things but find another way their beginnings are ontop of each other like columns. Make sure their highlight from hovering over their fields in their human readable format will still work.
- Headers Should be collapseable

	Console:
- Left Logging console should display the new table ID of the sent packet when it was sent instead of just "Packet sent" it should display something like "Packet <ID> sent"

########################

- For calculation of the SEQ and ACK numbers we should track the connections' most recent numbers and calculate the right value off of them. Make sure to use the connection from the selected packet.

- Also we should revert all the changes which enabled sending custom packets with the capture not running to make sure the above works correctly.

- Also the hex view with the offset/hex/ascii does still not display the ascii-part line starts above each other, currently they are just directly appended (seperated using two spaces) to the hex except for the last row's ascii. Maybe we should make this hex view three columns so the line starts are guaranteed to be above each other? Make sure to keep the highlight in the HEX when hovering over an element of the right sides human readable format field. Make sure the line starts of the different columns match. 

- For the UI, the seperators we currently use to change the sizes of the different windows are not alike. the one seperating the console from the right side is not the right color as the others. We want all of the current seperators used to resize the contained windows to be a thin black line with a margin instead of a full block of color. The lines should connect with the other seperators lines though.

- Also the colors of the hex views background is too yellow in comparison to the other fields backgrounds, it should be the same as the other windows. Also the scrollbar background of the console log window should be the same color as the other scrollbar backgrounds (look at packet lists scrollbar color for orientation). This color should also be used for the packet tables columns' background. When adjusting the colors make sure to use variables that are stored together so we can support different themes later on.

- Also remove the "Hex" checkbox and its label. Make sure the custom payload inputbox uses the new space between it and the "send custom" button

- Also currently there is a headbar which displays "File" and "Help" dropdown windows. remove these options and add "Settings" instead with one option when opening it, "Theme". When hovering over the "Theme" setting it should display the next dropdown with all the possible themes listed to the right of it. Maybe "dropdown window" is not the right name but you probably get what i mean. Also add two themes. One "White" which will be the current theme, and one "Dark". Make sure the "Dark" option also applies dark colors to all the elements